{
  "name": "agent-tracking-system",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first agent-work tracking for Rise development.",
  "scripts": { "check": "node --check packages/event-schema/index.js" },
  "engines": { "node": ">=20" }
}
